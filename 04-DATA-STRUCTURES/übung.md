# Übung Data-structures
- Kompilieren Sie die removeSharpS.ts über die Kommandozeile, so dass das man das ts auch im Browser debuggen kann
- Verfolgen Sie den Code über Debugging im Browser (oder im vscode, auch dort sollten Breakpoints funktionieren). 
    - Die ß werden tatsächlich entfernt. Was ist passiert?
- Fixen Sie die App, so dass die Layout-Struktur des html erhalten bleibt